experiment_name: 'classificaton_project' # name of the experiment to keep track in MLFlow UI
train_path: 'data/train' # path to train data
tags_path: 'data/tags.npy' # path to tags
pca_n_components: 120 # number of components for PCA to reduce the dimensionality of sentence embeddings. 
                      # recommendations to choose n preserving at least 80% of variance for sentence embeddings.
models: # Models of interest for training and evaluation.
  - LogisticRegression
  - SVC